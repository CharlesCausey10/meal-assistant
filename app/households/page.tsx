import { AccountTab } from '../account-tab'

export default function HouseholdsPage() {
    return (
        <main className="min-h-dvh bg-app-bg text-app-text">
            <AccountTab showBackLink />
        </main>
    )
}
